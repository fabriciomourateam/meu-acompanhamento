# SESSION HANDOFF — estado rolante entre janelas de contexto

> Leia este arquivo PRIMEIRO ao abrir uma nova janela (ver regra no `CLAUDE.md` →
> "Continuidade entre janelas de contexto"). Sobrescreva-o a cada handoff.
> Um SessionStart hook injeta este conteúdo automaticamente no início da sessão.

_Atualizado: 2026-06-21 (BRT)_

## Estado

Sem tarefa ativa neste repo (`meu-acompanhamento`) no momento. Este arquivo foi
criado junto com a regra de handoff + o SessionStart hook. A próxima sessão que
trabalhar aqui deve sobrescrever este conteúdo com o estado real.

## Gotchas / contexto do repo

- `meu-acompanhamento` é o **front-end do aluno** (lê do Supabase); o **MyShape
  (`controle-de-pacientes`)** é quem escreve. Ver `CLAUDE.md` → "Arquitetura do produto".
- Projeto Supabase: **"Controle de pacientes"** (`qhzifnyjyxdushxorzrk`, `sa-east-1`).
- Nome do dono: sempre "Fabricio" (sem acento) em texto novo.
- Existe um `docs/SESSION-HANDOFF.md` (hífen) legado de sessões antigas — o handoff
  rolante novo é este (`SESSION_HANDOFF.md`, underscore).
